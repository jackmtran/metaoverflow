extends layout.pug
include utils.pug
block content
  table
    thead
        tr
            th title
            th questions
            th Total Votes
    tbody
            tr
                td= questions.title
                td= questions.question
                td= questions.voteCount
                     form(method="DELETE", action=`/questions/${questions.id}`)
                        button(type='submit') delete
