extends layout.pug
include utils.pug
block content
  table
    thead
        tr
            th title
            th questions
            th Total Votes
    tbody
        each q in questions
            tr
                td
                  a(href=`/questions/${q.id}`) #{q.title}
                td #{q.question}
                td= q.voteCount
